title = "Hermes Drop gitleaks configuration"

# Published RFC 9180 interoperability vectors intentionally contain fixed
# cryptographic test material. They are public test vectors, not credentials.
[allowlist]
description = "Public RFC/CFRG HPKE test vectors"
paths = [
  '''^test/fixtures/hpke-vectors\.js$''',
]
